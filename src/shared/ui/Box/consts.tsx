const boxThemes = {
  gray: 'gray',
  green: 'green',
};

const boxTypes = {
  circle: 'circle',
};

const boxSizes = {
  30: '30',
  fitContent: 'fit-content',
};

const boxProps = {
  themes: boxThemes,
  types: boxTypes,
  sizes: boxSizes,
};

export { boxProps };
